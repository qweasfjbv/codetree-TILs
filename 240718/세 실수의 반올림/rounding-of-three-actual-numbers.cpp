#include <iostream>
#include <iomanip>
using namespace std;

int main() {
    // 여기에 코드를 작성해주세요.
    double a, b, c;
    cin >> a>> b >> c;

    cout << fixed << setprecision(3) << a << '\n' << b << '\n' << c;
    return 0;
}